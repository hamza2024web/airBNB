import React from 'react';
import { Receipt } from 'lucide-react';

const OrderDetailsCard = () => {
  // Simulating session data
  const sessionData = {
    prixTotale: '2500.00',
    numberOfDays: '7',
    dateStart: '2024-02-16',
    dateFine: '2024-02-23',
    prix: '357.14',
    days: '7',
    orderId: 'ORD-2024-001'
  };

  return (
    <div className="w-72 h-auto bg-green-800 rounded-lg p-6 flex flex-col items-center relative overflow-hidden shadow-2xl">
      {/* Enhanced shadow effect with multiple layers */}
      <div className="absolute inset-0 shadow-inner"></div>
      
      {/* Diagonal lines background */}
      <div className="absolute inset-0">
        {[...Array(5)].map((_, i) => (
          <div
            key={i}
            className="absolute h-1 w-full bg-green-700 opacity-20"
            style={{
              transform: `rotate(-45deg) translateY(${i * 40}px)`,
            }}
          />
        ))}
      </div>

      {/* Content */}
      <div className="z-10 flex flex-col items-center space-y-4 w-full">
        {/* Icon Circle */}
        <div className="w-24 h-24 rounded-full bg-gray-200 border-4 border-green-400 flex items-center justify-center shadow-lg">
          <Receipt className="w-16 h-16 text-green-800" />
        </div>

        {/* Order Info */}
        <div className="text-center space-y-1">
          <h2 className="text-xl font-bold text-white">Order Details</h2>
          <p className="text-sm text-green-300">#{sessionData.orderId}</p>
        </div>

        {/* Details */}
        <div className="w-full space-y-3 mt-4 bg-green-700/30 p-4 rounded-lg shadow-inner">
          <div className="flex justify-between text-sm text-white">
            <span>Total Price:</span>
            <span className="font-semibold">${sessionData.prixTotale}</span>
          </div>
          <div className="flex justify-between text-sm text-white">
            <span>Daily Rate:</span>
            <span className="font-semibold">${sessionData.prix}</span>
          </div>
          <div className="flex justify-between text-sm text-white">
            <span>Number of Days:</span>
            <span className="font-semibold">{sessionData.numberOfDays}</span>
          </div>
          <div className="flex justify-between text-sm text-white">
            <span>Start Date:</span>
            <span className="font-semibold">{sessionData.dateStart}</span>
          </div>
          <div className="flex justify-between text-sm text-white">
            <span>End Date:</span>
            <span className="font-semibold">{sessionData.dateFine}</span>
          </div>
        </div>

        {/* Barcode */}
        <div className="mt-6 w-full">
          <div className="h-12 bg-white rounded flex items-center justify-center shadow-md">
            <div className="w-4/5 h-8 bg-contain bg-no-repeat bg-center"
                 style={{
                   backgroundImage: `url("data:image/svg+xml,%3Csvg width='100' height='40' viewBox='0 0 100 40' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='3' height='40' fill='black'/%3E%3Crect x='5' width='1' height='40' fill='black'/%3E%3Crect x='8' width='2' height='40' fill='black'/%3E%3Crect x='12' width='3' height='40' fill='black'/%3E%3Crect x='17' width='2' height='40' fill='black'/%3E%3Crect x='21' width='1' height='40' fill='black'/%3E%3Crect x='24' width='3' height='40' fill='black'/%3E%3C/svg%3E")`
                 }}
            />
          </div>
        </div>
      </div>
    </div>
  );
};

